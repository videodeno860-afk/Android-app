name: Scientist AI Ultimate Error Free Factory

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build_complete_app:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Minimal Workspace
      uses: actions/checkout@v4

    - name: Setup Java JDK 17 Environment
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Build Pure Android Project From Scratch
      run: |
        echo "Creating strict separate directory structure..."
        mkdir -p app/src/main/java/com/ai/scientist/inventor
        mkdir -p app/src/main/res/layout
        mkdir -p app/src/main/res/values

        # 1. ROOT GRADLE CONFIGURATION (Locked Stable Versions)
        cat << 'EOF' > build.gradle
        buildscript {
            repositories { google(); mavenCentral() }
            dependencies { classpath 'com.android.tools.build:gradle:8.2.2' }
        }
        allprojects {
            repositories { google(); mavenCentral() }
        }
        EOF

        # 2. APP MODULE BUILD GRADLE (Minimal Clean Configuration)
        cat << 'EOF' > app/build.gradle
        plugins { id 'com.android.application' }
        android {
            namespace 'com.ai.scientist.inventor'
            compileSdk 34
            defaultConfig {
                applicationId "com.ai.scientist.inventor"
                minSdk 26
                targetSdk 34
                versionCode 1
                versionName "1.0.0"
            }
            buildTypes { release { minifyEnabled false } }
        }
        dependencies {
            // Using standard Android base libraries to prevent any resource conflicts
            implementation 'androidx.core:core:1.12.0'
        }
        EOF

        echo "include ':app'" > settings.gradle

        # 3. UNIVERSAL STRINGS AND BASIC RESOURCE STYLES
        cat << 'EOF' > app/src/main/res/values/strings.xml
        <resources>
            <string name="app_name">Scientist AI</string>
        </resources>
        EOF

        cat << 'EOF' > app/src/main/res/values/themes.xml
        <resources>
            <style name="Theme.ScientistAI" parent="android:Theme.DeviceDefault.NoActionBar">
                <item name="android:windowBackground">#121212</item>
            </style>
        </resources>
        EOF

        # 4. ALL SEPARATE INDIVIDUAL UI SCREENS XML LAYOUTS

        # Layout 1: Main Dashboard Screen
        cat << 'EOF' > app/src/main/res/layout/activity_main.xml
        <?xml version="1.0" encoding="utf-8"?>
        <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:padding="20dp"
            android:gravity="center"
            android:background="#121212">
            <TextView android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="🔬 SCIENTIST AI CORE" android:textColor="#FFFFFF" android:textSize="26sp" android:layout_marginBottom="30dp"/>
            <Button android:id="@+id/btn_signup" android:layout_width="match_parent" android:layout_height="wrap_content" android:text="1. Signup Page" android:layout_marginBottom="10dp"/>
            <Button android:id="@+id/btn_wallet" android:layout_width="match_parent" android:layout_height="wrap_content" android:text="2. Wallet Page" android:layout_marginBottom="10dp"/>
            <Button android:id="@+id/btn_home" android:layout_width="match_parent" android:layout_height="wrap_content" android:text="3. Home/3D Canvas" android:layout_marginBottom="10dp"/>
            <Button android:id="@+id/btn_projects" android:layout_width="match_parent" android:layout_height="wrap_content" android:text="4. Saved Project List" android:layout_marginBottom="10dp"/>
            <Button android:id="@+id/btn_profile" android:layout_width="match_parent" android:layout_height="wrap_content" android:text="5. Profile Page" android:layout_marginBottom="10dp"/>
            <Button android:id="@+id/btn_chat" android:layout_width="match_parent" android:layout_height="wrap_content" android:text="6. Chat Terminal"/>
        </LinearLayout>
        EOF

        # Layout 2: Signup Vault
        cat << 'EOF' > app/src/main/res/layout/activity_signup.xml
        <?xml version="1.0" encoding="utf-8"?>
        <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:padding="24dp"
            android:background="#1E1E1E">
            <TextView android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="🔒 Secure Signup Vault" android:textColor="#FFFFFF" android:textSize="22sp" android:layout_marginBottom="16dp"/>
            <EditText android:layout_width="match_parent" android:layout_height="wrap_content" android:hint="Enter Account / Details" android:textColorHint="#888888" android:textColor="#FFFFFF"/>
        </LinearLayout>
        EOF

        # Layout 3: Wallet Tracker
        cat << 'EOF' > app/src/main/res/layout/activity_wallet.xml
        <?xml version="1.0" encoding="utf-8"?>
        <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:padding="24dp"
            android:background="#1E1E1E">
            <TextView android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="💰 Balance Monitor" android:textColor="#FFFFFF" android:textSize="20sp"/>
            <TextView android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="USD Balance: $0.00" android:textColor="#00FF00" android:layout_marginTop="16dp"/>
        </LinearLayout>
        EOF

        # Layout 4: Home Canvas Workspace
        cat << 'EOF' > app/src/main/res/layout/activity_home_canvas.xml
        <?xml version="1.0" encoding="utf-8"?>
        <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:background="#1E1E1E">
            <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content" android:orientation="horizontal" android:gravity="center" android:padding="12dp" android:background="#252525">
                <Button android:id="@+id/btn_undo" android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="↩ Undo" android:layout_marginEnd="10dp"/>
                <Button android:id="@+id/btn_redo" android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="↪ Redo" android:layout_marginEnd="10dp"/>
                <Button android:id="@+id/btn_save" android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="💾 Save"/>
            </LinearLayout>
            <View android:layout_width="match_parent" android:layout_height="250dp" android:background="#2D2D2D" android:layout_margin="16dp"/>
        </LinearLayout>
        EOF

        # Layout 5: Projects List View
        cat << 'EOF' > app/src/main/res/layout/activity_projects_list.xml
        <?xml version="1.0" encoding="utf-8"?>
        <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:padding="16dp"
            android:background="#121212">
            <TextView android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="📁 Invention Project Dashboard" android:textColor="#FFFFFF" android:textSize="22sp" android:layout_marginBottom="16dp"/>
            <TextView android:layout_width="match_parent" android:layout_height="wrap_content" android:text="🚀 Sync Terminal Active" android:textColor="#03DAC6" android:background="#1E1E1E" android:padding="16dp"/>
        </LinearLayout>
        EOF

        # Layout 6: Profile Portfolio View
        cat << 'EOF' > app/src/main/res/layout/activity_profile.xml
        <?xml version="1.0" encoding="utf-8"?>
        <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="24dp"
            android:background="#1E1E1E">
            <TextView android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="👤 Profile Workspace" android:textColor="#FFFFFF" android:textSize="22sp"/>
        </LinearLayout>
        EOF

        # Layout 7: AI Chat Terminal
        cat << 'EOF' > app/src/main/res/layout/activity_chat.xml
        <?xml version="1.0" encoding="utf-8"?>
        <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:padding="16dp"
            android:background="#121212">
            <TextView android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="🧠 Scientist AI Core Chat" android:textColor="#FFFFFF" android:textSize="20sp"/>
            <View android:layout_width="match_parent" android:layout_height="0dp" android:layout_weight="1" android:background="#1E1E1E" android:layout_marginTop="12dp"/>
        </LinearLayout>
        EOF

        # 5. ALL JAVA RESTRUCTURING TO CONTROL RUNTIME
        cat << 'EOF' > app/src/main/java/com/ai/scientist/inventor/MainActivity.java
        package com.ai.scientist.inventor;
        import android.app.Activity;
        import android.content.Intent;
        import android.os.Bundle;

        public class MainActivity extends Activity {
            @Override
            protected void onCreate(Bundle savedInstanceState) {
                super.onCreate(savedInstanceState);
                setContentView(R.layout.activity_main);

                findViewById(R.id.btn_signup).setOnClickListener(v -> startActivity(new Intent(MainActivity.this, SignupActivity.class)));
                findViewById(R.id.btn_wallet).setOnClickListener(v -> startActivity(new Intent(MainActivity.this, WalletActivity.class)));
                findViewById(R.id.btn_home).setOnClickListener(v -> startActivity(new Intent(MainActivity.this, HomeCanvasActivity.class)));
                findViewById(R.id.btn_projects).setOnClickListener(v -> startActivity(new Intent(MainActivity.this, ProjectsListActivity.class)));
                findViewById(R.id.btn_profile).setOnClickListener(v -> startActivity(new Intent(MainActivity.this, ProfileActivity.class)));
                findViewById(R.id.btn_chat).setOnClickListener(v -> startActivity(new Intent(MainActivity.this, ChatActivity.class)));
            }
        }
        EOF

        cat << 'EOF' > app/src/main/java/com/ai/scientist/inventor/HomeCanvasActivity.java
        package com.ai.scientist.inventor;
        import android.app.Activity;
        import android.os.Bundle;
        import android.widget.Toast;

        public class HomeCanvasActivity extends Activity {
            @Override
            protected void onCreate(Bundle savedInstanceState) {
                super.onCreate(savedInstanceState);
                setContentView(R.layout.activity_home_canvas);
                findViewById(R.id.btn_undo).setOnClickListener(v -> Toast.makeText(this, "Undo triggered", Toast.LENGTH_SHORT).show());
                findViewById(R.id.btn_redo).setOnClickListener(v -> Toast.makeText(this, "Redo triggered", Toast.LENGTH_SHORT).show());
                findViewById(R.id.btn_save).setOnClickListener(v -> Toast.makeText(this, "Invention Saved!", Toast.LENGTH_SHORT).show());
            }
        }
        EOF

        echo -e "package com.ai.scientist.inventor;\nimport android.app.Activity;\nimport android.os.Bundle;\npublic class SignupActivity extends Activity { @Override protected void onCreate(Bundle b) { super.onCreate(b); setContentView(R.layout.activity_signup); } }" > app/src/main/java/com/ai/scientist/inventor/SignupActivity.java
        echo -e "package com.ai.scientist.inventor;\nimport android.app.Activity;\nimport android.os.Bundle;\npublic class WalletActivity extends Activity { @Override protected void onCreate(Bundle b) { super.onCreate(b); setContentView(R.layout.activity_wallet); } }" > app/src/main/java/com/ai/scientist/inventor/WalletActivity.java
        echo -e "package com.ai.scientist.inventor;\nimport android.app.Activity;\nimport android.os.Bundle;\npublic class ProjectsListActivity extends Activity { @Override protected void onCreate(Bundle b) { super.onCreate(b); setContentView(R.layout.activity_projects_list); } }" > app/src/main/java/com/ai/scientist/inventor/ProjectsListActivity.java
        echo -e "package com.ai.scientist.inventor;\nimport android.app.Activity;\nimport android.os.Bundle;\npublic class ProfileActivity extends Activity { @Override protected void onCreate(Bundle b) { super.onCreate(b); setContentView(R.layout.activity_profile); } }" > app/src/main/java/com/ai/scientist/inventor/ProfileActivity.java
        echo -e "package com.ai.scientist.inventor;\nimport android.app.Activity;\nimport android.os.Bundle;\npublic class ChatActivity extends Activity { @Override protected void onCreate(Bundle b) { super.onCreate(b); setContentView(R.layout.activity_chat); } }" > app/src/main/java/com/ai/scientist/inventor/ChatActivity.java

        # 6. MASTER CLEAN ANDROID MANIFEST PIPELINE
        cat << 'EOF' > app/src/main/AndroidManifest.xml
        <?xml version="1.0" encoding="utf-8"?>
        <manifest xmlns:android="http://schemas.android.com/apk/res/android">
            <uses-permission android:name="android.permission.INTERNET" />
            <application
                android:allowBackup="true"
                android:label="@string/app_name"
                android:supportsRtl="true"
                android:theme="@style/Theme.ScientistAI">
                <activity android:name=".MainActivity" android:exported="true">
                    <intent-filter>
                        <action android:name="android.intent.action.MAIN" />
                        <category android:name="android.intent.category.LAUNCHER" />
                    </intent-filter>
                </activity>
                <activity android:name=".SignupActivity" />
                <activity android:name=".WalletActivity" />
                <activity android:name=".HomeCanvasActivity" />
                <activity android:name=".ProjectsListActivity" />
                <activity android:name=".ProfileActivity" />
                <activity android:name=".ChatActivity" />
            </application>
        </manifest>
        EOF

        gradle wrapper --gradle-version 8.2.2
        chmod +x gradlew
        echo "Source Files Configured Cleanly."

    - name: Run Direct Gradle Compile Engine
      run: ./gradlew assembleDebug --no-daemon

    - name: Convert Workspace to Backup ZIP
      run: |
        sudo apt-get install -y zip
        zip -r scientist_ai_clean_source.zip . -x "*.git*" -x "*.gradle*" -x "*/build/*"

    - name: Upload Final Unsigned APK Product
      uses: actions/upload-artifact@v4
      with:
        name: Scientist-AI-Production-APK
        path: app/build/outputs/apk/debug/*.apk

    - name: Upload Editable Full ZIP Backup Bundle
      uses: actions/upload-artifact@v4
      with:
        name: Scientist-AI-Source-ZIP
        path: scientist_ai_clean_source.zip
        
